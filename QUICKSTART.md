# 🚀 Быстрый старт

## За 5 минут до первой ссылки

### Шаг 1: Проверка окружения

```bash
# Проверка Java (должна быть версия 17 или выше)
java -version

# Проверка Maven (должна быть версия 3.8+)
mvn -version
```

Если что-то не установлено:
- Java: https://adoptium.net/
- Maven: https://maven.apache.org/download.cgi

### Шаг 2: Сборка проекта

```bash
# Клонируйте репозиторий (или используйте существующую директорию)
cd /Users/vasyan/labs/link_shorter

# Соберите проект
mvn clean package

# Или используйте готовый скрипт
chmod +x run.sh
./run.sh
```

### Шаг 3: Первый запуск

После сборки увидите приветствие:

```
╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║              СЕРВИС СОКРАЩЕНИЯ ССЫЛОК / LINK SHORTENER               ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝

Добро пожаловать!
Ваш UUID: 12345678-1234-1234-1234-123456789abc

Введите 'help' для просмотра доступных команд.
> 
```

**Важно**: Сохраните ваш UUID для последующих сессий!

### Шаг 4: Создание первой ссылки

```bash
> create https://www.baeldung.com/java-9-http-client

✓ Короткая ссылка успешно создана!
  Короткий код: aBc123
  Полная короткая ссылка: clck.ru/aBc123
  Лимит переходов: 100
  Время жизни: 24 часов
```

### Шаг 5: Открытие ссылки

```bash
> open aBc123

Переход по ссылке: https://www.baeldung.com/java-9-http-client
✓ Ссылка открыта в браузере
```

Браузер автоматически откроется с вашим URL!

## 📖 Основные команды

### Создание ссылки с лимитом

```bash
> create https://github.com/yourname/repo 50
```

Создаст ссылку с лимитом 50 переходов.

### Просмотр всех ссылок

```bash
> list
```

Увидите таблицу со всеми вашими ссылками.

### Информация о ссылке

```bash
> info aBc123
```

Подробная информация о конкретной ссылке.

### Обновление лимита

```bash
> update aBc123 200
```

Изменит лимит на 200 переходов.

### Удаление ссылки

```bash
> delete aBc123
```

Безвозвратно удалит ссылку.

### Справка

```bash
> help
```

Полный список команд с примерами.

### Выход

```bash
> exit
```

## 💡 Советы

### 1. Сохраните свой UUID

Чтобы продолжить работу позже:

```bash
# В первой сессии узнайте свой UUID
> user
Текущий пользователь:
  UUID: 12345678-1234-1234-1234-123456789abc

# В следующей сессии запустите с UUID
java -jar target/link-shorter-1.0.0-jar-with-dependencies.jar 12345678-1234-1234-1234-123456789abc
```

### 2. Создавайте ссылки с разными лимитами

```bash
# Для публичной ссылки с большим лимитом
> create https://public-resource.com 1000

# Для приватной ссылки на один раз
> create https://secret-doc.com 1
```

### 3. Мониторьте использование

```bash
# Регулярно проверяйте статус ваших ссылок
> list

# Смотрите детали конкретной ссылки
> info aBc123
  Переходов: 45 / 100 (осталось: 55)
```

### 4. Используйте автоматическую очистку

Истёкшие ссылки автоматически удаляются каждый час. Следите за сообщениями:

```
[Cleanup] Удалено истёкших ссылок: 3
```

## 🔧 Настройка

Отредактируйте `src/main/resources/application.properties`:

```properties
# Время жизни по умолчанию (24 часа в миллисекундах)
link.ttl.default=86400000

# Лимит переходов по умолчанию
link.click.limit.default=100

# Длина короткого кода
link.code.length=6
```

После изменения пересоберите проект:

```bash
mvn clean package
```

## 🧪 Запуск тестов

```bash
# Все тесты
mvn test

# С отчётом о покрытии
./test.sh

# Откроется HTML отчёт в браузере
```

## ❓ Часто задаваемые вопросы

### Q: Могу ли я создать несколько ссылок на один URL?

**A**: Да! Каждый раз получите новый уникальный короткий код для одного пользователя.

### Q: Что если я потерял UUID?

**A**: К сожалению, без UUID доступ к старым ссылкам невозможен. Сохраняйте UUID в надёжном месте!

### Q: Можно ли изменить время жизни конкретной ссылки?

**A**: В текущей версии TTL задаётся глобально в конфигурации. Индивидуальные TTL можно добавить в будущих версиях.

### Q: Сколько ссылок я могу создать?

**A**: Ограничений нет (кроме доступной памяти). Но учитывайте, что хранилище in-memory, данные не сохраняются после перезапуска.

### Q: Можно ли использовать на сервере?

**A**: Да! Запустите в tmux/screen сессии:

```bash
# В tmux
tmux new -s linkshorter
java -jar link-shorter-1.0.0-jar-with-dependencies.jar
# Ctrl+B, D для отсоединения
```

## 📚 Дальнейшее чтение

- [README.md](README.md) - Полная документация с разделом о тестировании

## 🐛 Проблемы?

1. Убедитесь, что используете Java 17+
2. Проверьте, что Maven установлен
3. Пересоберите проект: `mvn clean package`
4. Проверьте логи ошибок
5. Создайте Issue на GitHub

---

**Happy Link Shortening! 🎉**

Теперь вы готовы использовать сервис сокращения ссылок!

